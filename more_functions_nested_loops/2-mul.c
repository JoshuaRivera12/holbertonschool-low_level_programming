#include "main.h"

/**
* mul- multiplys both numbers
* @a: is the first number being multiplied
* @b: is the second number being mulitplied
*
* Return: the product of a & b multiplied
*/

int mul(int a, int b)
{
	return (a * b);
}

#include "main.h"
/**
* print_most_numbers - writes a function that prints most numbers
*
* Return: Always 0
*/

void print_most_numbers(void)
{
	{
	for (n = '0'; i <= '9'; n++)
		if (n == '2' || i == '4')
			continue;
	_putchar(n)
	}
	_putchar('\n')
}
